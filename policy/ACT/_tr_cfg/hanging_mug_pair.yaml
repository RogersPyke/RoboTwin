# Multi-task training config (example)
# Run: bash train.sh --config example
# Run process_data.sh for each task first.

TRAIN_TASKS:
  - [hanging_mug, demo_clean, 100]
  - [unhanging_mug, demo_clean, 100]
TRAIN_SEED: 0
TRAIN_GPU_ID: 0
